# AI environment settings are separated by deployment target.
#
# Section 1: Development
# See the complete AI block in .env.example.
#
# Section 2: Render production
# See the complete AI block in .env.render.example.
#
# Keep the two environments separate; do not load both files into one process.
